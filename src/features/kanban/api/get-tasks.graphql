query GetTasks($input: FilterTaskInput!) {
  tasks(input: $input) {
    dueDate
    id
    name
    pointEstimate
    position
    status
    tags
    assignee {
      avatar
      fullName
      id
    }
  }
}
