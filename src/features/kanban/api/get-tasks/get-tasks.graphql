fragment Task on Task {
  dueDate
  id
  name
  pointEstimate
  position
  status
  tags
  assignee {
    avatar
    fullName
    id
  }
}

query GetTasks($input: FilterTaskInput!) {
  tasks(input: $input) {
    ...Task
  }
}
